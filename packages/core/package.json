{
	"name": "@webhook/core",
	"module": "src/index.ts",
	"type": "module",
	"private": true,
	"scripts": {
		"build": "bun build --target bun --minify --outdir dist src/index.ts ",
		"fmt": "biome format --write",
		"lint": "biome lint --write",
		"typecheck": "tsc --noEmit",
		"check": "bun run fmt; bun run lint; bun run typecheck"
	}
}
